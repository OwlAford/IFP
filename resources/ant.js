import { Select, TreeSelect, Radio, Button, Table, DatePicker, Modal, Tree, Input, Icon, message, Spin, Form, Col, Row, notification } from 'antd'

const ant = {
  Select, 
  TreeSelect,	
  Radio,
  Col, 
  Row,	
  Button, 
  Table, 
  Modal,
  Tree,
  Form, 
  Input, 
  Icon, 
  message,
  Spin,
  DatePicker,
  notification 
}

export default ant