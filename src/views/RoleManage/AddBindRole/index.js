import { connect } from 'react-redux'
import { } from 'REDUCER/roleManage'
import AddBindRoleView from './AddBindRoleView'

const mapDispatchToProps = {

}

const mapStateToProps = (state) => ({

})

export default connect(mapStateToProps, mapDispatchToProps)(AddBindRoleView)
